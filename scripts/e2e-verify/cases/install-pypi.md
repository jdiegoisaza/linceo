# uv tool install linceo (from PyPI)

**Status:** ok
**Exit code:** 0

```bash
uv tool install linceo
```

<details><summary>stderr</summary>

```
Resolved 4 packages
Installed 4 packages
 + click==8.5.0
 + linceo==0.9.0
 + typer-slim==0.21.1
 + typing-extensions==4.16.0
Installed 1 executable: linceo
warning: `<RUNDIR>/uv-tool-bare-bin` is not on your PATH. To use installed tools, run `export PATH="<RUNDIR>/uv-tool-bare-bin:$PATH"` or `uv tool update-shell`.
```

</details>
