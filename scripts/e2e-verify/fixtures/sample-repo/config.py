"""Fixture content for `scripts/e2e-verify`'s generated sample repository — not real code.

Carries a synthetic AWS-access-key-shaped string, matching gitleaks' own
`aws-access-token` rule — the exact same synthetic value already used in
tests/unit/fixtures/gitleaks/one_finding.json and tests/unit/test_cli_scan.py,
reused here instead of inventing a new one. Not a real credential.
"""

AWS_ACCESS_KEY_ID = "AKIAQPFM3ZXVJ7HKQZ2A"
